# Shape 创作图约束

状态：设计约束（Normative）  
范围：Scene、GraphComponent、Operator 工作区、执行器与外部创意工具的边界。  
非范围：本文件不证明任何规划中的能力已经实现；实现状态仍以相邻源码、测试与
[`DEV_SKELETON.md`](DEV_SKELETON.md) 为准。

## 1. 核心分层

Shape 采用三层模型，三者不得互相冒充：

1. **创作图（Scene Operator Graph）**是用户创作意图的图。它只连接类型化的
   Source、创作级 Operator 与命名 Output。
2. **Operator 工作区**是完成一个创作步骤的交互式界面。它拥有素材选择、自然语言
   意图、编辑工具、候选比较和锁定动作。
3. **执行图（Execution Plan）**是实现一次运行的物理计划。模型、采样器、缓存、
   重试、转码、Infer Job、ComfyUI 工作流和外部工具调用都属于此层。

创作图回答“想创作什么、各部分如何相连”；执行图回答“这次具体怎样得到结果”。
后者可以很复杂，但默认不可见，也不得成为 Project 的创作语义。

## 2. 图中允许出现什么

一个步骤只有同时满足下列条件，才应提升为 Scene 节点：

- 有独立、可说明的创作目的；
- 有稳定的类型化输入和输出边界；
- 值得被连接、复用、单独审阅、分支或替换；
- 其完成状态对创作结构有意义，而不仅是某个工具的内部手势。

因此 `AI Text Editing`、`Image Editing`、`AI Image Creation`、外部编辑会话和跨媒介
合成可以是节点；Crop、Resize、Blur、Rewrite、Expand、Polish、模型路由和重采样通常
是节点工作区或执行图内部的能力。内部能力仍可拥有严格、版本化的技术合同，但不能仅因
实现存在就自动进入主画布。

## 3. 意图、候选与已接受结果

- Operator 保存的是可复用的**意图**和其输入绑定，而不是一次模型运行的偶然结果。
- 一次成功执行只产生 Candidate/Preview；它既不推进 Output，也不覆盖现有 Artifact
  Revision。
- 只有用户的显式 Accept 才能以 expected-head CAS 推进不可变 Artifact Revision。
- Candidate 必须能够指向其输入修订、作者意图和 payload-free 的执行 receipt；Runtime
  Attempt 成功不等于用户接受。
- 同一意图可以产生多个候选。候选数量属于执行/审阅策略，不改变创作图端口数量。

这使 Shape 同时具备达芬奇式非破坏编辑与可复跑的生成历史，而不是把“运行一次”误写为
“永久修改内容”。

## 4. 渐进式界面披露

每个 Operator 至少具有三种信息密度，且默认从低到高披露：

| 表面 | 必须表达 | 不应表达 |
| --- | --- | --- |
| 画布卡片 | 角色、标题、媒体类型、简短真实预览、执行/候选状态、可连接端口 | 长 prompt、完整正文、工具参数、执行日志 |
| 聚焦工作区 | 素材、意图、专业编辑交互、候选、比较、锁定 | 不相关的其它节点配置 |
| Execution Inspector | 实际执行计划、缓存、路由、模型/外部工具 provenance、稳定错误码 | 对普通创作流程的强制干扰 |

画布不是缩小版表单：卡片必须保持可扫读、内容裁切且不溢出。完整文本、图像、波形和
多版本审阅属于聚焦工作区。

## 5. GraphComponent 与可组合性

GraphComponent 是可复用的创作级子图，而非暴露任意微操作的“宏录制”。它必须：

- 只公开版本化、命名且类型化的输入和输出；
- 封装内部节点、布局和执行策略，调用者不能依赖其内部实现；
- 具备明确的版本/迁移策略；
- 不捕获 provider 专属模型名、临时 endpoint、凭据或未接受的 Candidate；
- 将内部运行记录保留在 receipt/provenance 中，而不泄漏为调用者的持久图结构。

GraphComponent 让 Shape 获得类似 ComfyUI 子图的复用能力，同时保留创作级可读性。

## 6. 执行与批量生产

执行器可以构造任意严格验证过的 DAG，并进行部分重跑、内容寻址缓存、队列、重试和
批量变体生产。它必须满足：

- 计划由已绑定的输入修订和已持久化的 Operator 意图编译；QML 投影不能成为执行权威；
- 结果必须重新通过 Shape 的内容/类型边界校验，再成为 Candidate；
- 一次运行可追溯，但模型、provider 与低层参数不进入创作图身份；
- 批量与参数矩阵属于专门的 Production Surface，不应压入日常画布；
- 不可复用的瞬态缓存、运行失败与上游服务状态绝不破坏既有已接受结果。

未来可以给高级用户提供只读的 Execution Inspector 与受约束的批量配方；这不是把默认
画布变成 ComfyUI 参数面板。

## 7. 外部编辑器与适配器

Shadow、Echo 或未来执行器通过窄 Adapter 接入，而不共享数据库或夺取项目所有权：

- Adapter 固定 Shape 的输入 Artifact Revision 与媒体合同；
- 外部原始素材始终为只读引用；
- Adapter 返回的字节、元数据和 receipt 必须重新验证；
- 成功只创建 Shape Candidate，Accept 后才更新 Shape Artifact；
- 外部应用不可直接修改 Shape Scene、Candidate Shelf 或已接受 Revision。

因此 Shape 可以把调色、复杂音频编辑等交给更合适的产品，而不会把不可追溯的副作用
混入创作图。

## 8. 新能力的设计检查

新增任何节点、工具或外部接入前，必须回答：

1. 它是独立创作意图，还是现有 Operator 的内部能力？
2. 它的输入、输出、候选和 Accept 边界分别是什么？
3. 用户在画布上需要看见的最小真实摘要是什么？
4. 执行图中哪些细节必须可追溯，哪些细节必须保持折叠？
5. 它是否需要 GraphComponent、Production Surface 或外部 Adapter，而不是新原子节点？
6. 失败、重跑、缓存失效或外部不可用时，已接受内容是否仍可查看和导出？

若这些问题没有明确答案，能力不得以“先占一个节点位置”的方式进入主画布。

## 9. 明确不做的事

- 不把 Shape 变成 provider 工作流编辑器、ComfyUI 节点浏览器或通用 Filter SDK；
- 不把 Echo/Shadow 的媒体内部结构自动提升为 Scene 节点；
- 不用成功运行替代用户的 Accept；
- 不将凭据、endpoint、模型物理名称或特定执行器参数固化为创作语义；
- 不以伪造的多输入/多输出投影掩盖尚未实现的持久 Scene 图能力。

